#include <stdio.h>

int main()
{
    char a;
    unsigned char b, c;

    /*
    scanf("%c", &a);
    printf("%d", a);
    */

    scanf("%c %c %c", &a, &b, &c);

    printf("%d %d %d", a, b, c);
    

    return 0;
}